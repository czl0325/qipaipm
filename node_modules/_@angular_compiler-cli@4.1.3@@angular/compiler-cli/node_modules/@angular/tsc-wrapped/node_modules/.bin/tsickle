#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../../_tsickle@0.21.6@tsickle/build/src/main.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../../_tsickle@0.21.6@tsickle/build/src/main.js" "$@"
  ret=$?
fi
exit $ret
